
/**
 * Aggregator for all native libraries that are conditional dependencies.
 */
module io.avaje.webview.platform {
  requires transitive io.avaje.webview;
  // requires static io.avaje.webview.linux32;
  // requires static io.avaje.webview.linux64;
  // requires static io.avaje.webview.windows;
  // requires static io.avaje.webview.mac;
}